		 load('../data/Result.txt','ascii')
         R=30
         C=R
         Z=R;
		 [X,Y,Z] = meshgrid([1:R],[1:C],[1:Z]);
		 xslice = [1,R/2,R];   
		 yslice = [];
		 zslice = R/2;
		 slice(X,Y,Z,stack,xslice,yslice,zslice)